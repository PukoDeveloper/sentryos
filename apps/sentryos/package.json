{
  "name": "sentryos",
  "version": "1.0.0",
  "type": "module",
  "description": "SentryOS — A lightweight plugin-based OS-like runtime for the browser",
  "license": "MIT",
  "module": "./dist/sentryos.js",
  "types": "./dist/sentryos.d.ts",
  "exports": {
    ".": {
      "types": "./dist/sentryos.d.ts",
      "import": "./dist/sentryos.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build --mode lib",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^29.0.2",
    "typescript": "~5.9.3",
    "vite": "^7.2.4",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "quickjs-emscripten": "^0.32.0"
  },
  "peerDependencies": {
    "sentryos-sdk": ">=1.0.0"
  }
}
